# Student-Attendance-Tracker-DBMS-Project
Student Attendance Management System is a DBMS-based web application that manages student attendance using MySQL. It allows adding students, marking attendance, viewing records with JOIN operations, and deleting entries. Built using PHP, MySQL, and HTML/CSS
